\chapter{Methods}
\label{sec:Methods}

% ----------------------------------------
\section{Information Field Theory}
\label{sec:ift}

Rather than analyzing individual stars or predefined subgroups separately, this thesis combines
the incomplete velocity information of the whole catalog into one continuous, three-dimensional
velocity field that describes the motion of Taurus.

This is a common problem in physics: a continuous physical quantity, here a velocity
field, must be reconstructed from a finite, noisy set of measurements. Information field
theory (IFT) is a Bayesian framework designed specifically for this kind of problem
\citep{Ensslin2023_ITIFT,Ensslin2025}. Instead of treating the unknown field as a small
set of parameters, such as the coefficients of a simple analytic function, IFT treats it
as the object to be inferred directly. The field, denoted $s$, can take on any value at any
point in space and can therefore be seen as an infinite-dimensional random variable. Its
shape is constrained purely by the data $d$ and a set of prior assumptions.

Formally, the quantity of interest is the posterior probability $\mathcal{P}(s\mid d)$ of
the field given the data. Bayes' theorem relates this posterior to the likelihood
$\mathcal{P}(d\mid s)$, which describes how the data are generated from a given field
configuration, and to the prior $\mathcal{P}(s)$, which encodes expectations about the
field before any data are taken into account:
\begin{equation}
    \mathcal{P}(s \mid d) = \frac{\mathcal{P}(d \mid s)\,\mathcal{P}(s)}{\mathcal{P}(d)} \, .
    \label{eq:bayes}
\end{equation}
The evidence $\mathcal{P}(d)$ acts purely as a normalization constant and does not depend on
the field $s$. For the inference of $s$ it is therefore irrelevant and, as is common in many
Bayesian approaches, never has to be evaluated explicitly; only the shape of the posterior as
a function of $s$ matters. Rather than a single field, the posterior gives a probability for every field that could have
produced the data, and its scatter is the uncertainty of the reconstruction
\citep{Ensslin2023_ITIFT,Ensslin2025}.

In practice, writing the posterior in Eq.~(\ref{eq:bayes}) down analytically requires assumptions
this model does not make, such as a fixed correlation structure for the field.
IFT provides the mathematical machinery to approximate this posterior
numerically, and to do so in a way that also returns uncertainty estimates for the
reconstructed field, not just a single best-fit map.

% --------------------------------------------------------
\section{NIFTy - Numerical Information Field Theory}
\label{sec:nifty}

The numerical implementation of IFT used in this thesis is
\texttt{NIFTy}~\citep[\texttt{NIFTy.re};][]{Edenhofer2024}, a Python package built on
\texttt{JAX} that extends the original \texttt{NIFTy} framework \citep{niftycl} with
building blocks for correlated field models and for the variational inference
algorithms needed to approximate the posterior described before.

An important prior assumption of this model is that the velocity field is spatially
correlated: nearby points are expected to have similar velocities, while the field can vary
more or less smoothly depending on the physical scale considered. This kind of spatially
correlated field follows and adapts the approach of \citet{Hutschenreuter2026}, who used the same underlying
model and code to reconstruct the three-dimensional velocity field of the Sco-Cen OB
association. Given the modest size of the Taurus sample of a few hundred stars, compared to
over ten thousand for Sco-Cen, the model is kept
deliberately simple with each velocity component being described by a single correlated field,
without attempting to separate the region into several kinematically distinct populations, as
was done for Sco-Cen, since the data would likely not constrain such a model.

The three-dimensional velocity field is modeled on a regular grid of $42 \times 23 \times 27$
cells of $3~\mathrm{pc}$, spanning a volume of $126 \times 69 \times 81~\mathrm{pc}$ around Taurus.
Representing the field on this grid makes the inference computationally manageable, replacing an infinite
number of parameters with a finite, though still large, set of values. Each
velocity component is represented as a \texttt{NIFTy} correlated field, separated
into a constant offset $s_0$, corresponding to the expected bulk motion of Taurus as a
whole, and a spatially correlated deviation $\delta(\mathbf{x})$ from that offset,
\begin{equation}
    s(\mathbf{x}) = s_0 + \delta(\mathbf{x}) \, .
    \label{eq:correlatedfield}
\end{equation}
Rather than being built up cell by cell, the deviation is decomposed into Fourier modes,
plane waves of a given wavelength and direction from which any field on the grid can be
assembled. Short wavelengths carry the small-scale structure and long ones the large-scale
variation. In this
representation the deviation is generated as
\begin{equation}
    \delta(\mathbf{x}) = \mathrm{FT}^{-1}\!\left[\sqrt{P(k)}\,\xi(\mathbf{k})\right]\!(\mathbf{x}) \, ,
    \label{eq:deviation}
\end{equation}
where the $\xi(\mathbf{k})$ are independent random numbers drawn from a standard normal
distribution, one for each mode. They carry no structure themselves; it is the power spectrum
$P(k)$ that assigns each mode its typical amplitude on the spatial scale $k^{-1}$ and thereby fixes
the correlation structure of the field. The inverse
Fourier transform $\mathrm{FT}^{-1}$ then adds the weighted modes back together into a field in
real space.
The inference only has to find the $\xi$ that reproduce the
data instead of a correlated value in every grid cell. $P(k)$ itself is not fixed by hand: its
amplitude and slope are inferred from the data, so that the balance between small-scale and
large-scale structure is set by the observations rather than assumed a priori.

The likelihood does not compare the field to space velocities, but to the measured quantities.
For each star the field is evaluated at its position through interpolation
(further discussed in Chapter~\ref{sec:Results}) and projected onto the two proper-motion
components on the sky and, where available, the radial velocity along the line of sight, with
additive Gaussian measurement noise.
A star without a
radial velocity therefore constrains the field only within the plane of the sky, but since the
prior correlates neighboring positions, its missing component is supplied by the stars around
it. This is what allows the full sample to be used instead of only the part of it with
complete velocities.

The nominal
Gaia uncertainties do not necessarily capture the full observational error, for example
because unmodeled stellar peculiar motions or unresolved binary systems add scatter beyond the
reported values. The model can therefore rescale the noise level of the
radial velocities by an $\eta$ parameter per star that is estimated from the data.
This data-driven noise estimation is applied only to the radial-velocity measurements.
The proper motions are kept at their reported Gaia uncertainties.
Its usage is discussed in Chapter~\ref{sec:Results}, where its effects on the reconstruction are examined.

Given this likelihood and the correlated-field priors, the posterior distribution over the
velocity field is then approximated using \texttt{NIFTy}'s variational inference scheme,
geometric variational inference (geoVI), and metric Gaussian variational
inference (MGVI) \citep{Knollmueller2020,Frank2021}. Both approximate the posterior by a simpler
distribution $\mathcal{Q}(s)$, fit by minimizing the Kullback--Leibler divergence
\begin{equation}
    D_{\mathrm{KL}}(\mathcal{Q} \,\|\, \mathcal{P}) = \int \mathcal{D}s\; \mathcal{Q}(s)\,\ln\frac{\mathcal{Q}(s)}{\mathcal{P}(s\mid d)} \, ,
    \label{eq:kl}
\end{equation}
which is zero only if $\mathcal{Q}$ exactly matches the true posterior. Expressing the posterior
through Bayes' theorem (Eq.~(\ref{eq:bayes})) reduces the objective to quantities the model
provides explicitly, the likelihood and the prior, with the evidence $\mathcal{P}(d)$ remaining merely
as an additive constant.

In practice, the
algorithm iteratively refines this approximation by alternating between updating the most
probable field configuration and drawing an increasing number of posterior samples around
it, in order to capture the local shape of the posterior more accurately as the fit
converges. The final result of this procedure is a set of posterior samples of the full 3D
velocity field, from which both a mean velocity map and a corresponding uncertainty
(standard deviation) map are computed at every grid point, providing the reconstructed
velocity field of Taurus together with an estimate of its reliability
\citep{Knollmueller2020,Frank2021}.

% -----------------------------------------------------
\section{Dataset}
\label{sec:dataset}

The analysis is based on a catalog of 557 stars associated with the TMC,
identified with the \texttt{SigMA} clustering algorithm of
\citet{Ratzenboeck2023a_SigMA} applied to Gaia~DR3 astrometry \citep{GaiaCollaboration2023}.
The same membership catalog underlies the analysis of \citet{Alves2026}.
Members that also appear in the census of \citet{Luhman2023} are flagged as such.
For each star the catalog provides the full astrometric solution: equatorial
coordinates ($\alpha$, $\delta$), trigonometric parallax $\varpi$ and proper motions in
right ascension and declination ($\mu_{\alpha*}$, $\mu_\delta$) together with radial
velocities, where available, all with their respective uncertainties. The renormalized
unit weight error (RUWE) serves as a quality indicator for the astrometric solution,
quantifying how well the single-star model fits the data, with values close to one
indicating a good fit \citep{Pearce2022RUWE}.
All 557 stars possess proper motion measurements, while radial velocities are available
for only 180 of them, representing roughly one third of the sample
(Figure~\ref{fig:dataset_positions}). Photometric data are provided in the form of the
Gaia $G$-band magnitude and the colors $G-G_\mathrm{RP}$ and $G_\mathrm{BP}-G_\mathrm{RP}$.
Stellar ages, visual extinctions $A_V$, and masses are included for a subset of the
sample. They are obtained with the \texttt{Chronos} algorithm~\citep{Ratzenboeck2023b_Chronos},
which fits theoretical isochrones to the Gaia
color--magnitude diagram of each cluster while solving for extinction simultaneously,
so that every member of a cluster is assigned the same age.

The three-dimensional heliocentric Galactic Cartesian positions $(X, Y, Z)$ and
corresponding velocity components $(U, V, W)$ are computed from the astrometric and
radial-velocity data, with the $X$-axis pointing toward the Galactic center, $Y$ in the
direction of Galactic rotation, and $Z$ toward the Galactic north pole.

Before any field reconstruction, several properties of the data can already be analyzed
directly. Figure~\ref{fig:dataset_xz_age} shows the
stellar positions projected onto the $X$--$Z$ plane, colored by stellar age. The members are
not uniformly distributed but fall into several spatially separated groups that also differ in
age, with a visible age gradient between the two large subgroups and a small, spatially distinct
group of older stars (15~Myr).
Considering the velocity data, the proper motion in right ascension reveals the same division, so
that ages and velocities appear correlated (Figure~\ref{fig:pmra_xz}).

\begin{figure}[h!]
    \centering
    \begin{subfigure}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/xz_age.pdf}
        \caption{Stellar ages}
        \label{fig:dataset_xz_age}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/pmra_gradient.pdf}
        \caption{Right ascension proper motion}
        \label{fig:pmra_xz}
    \end{subfigure}
    \caption{Positions of the catalog stars projected onto the $X$--$Z$ plane, colored by
        stellar age (left) and right ascension proper motion data (right).
        Stars shown in grey have no data available. Several spatially
        separated groups are visible, indicating that star formation in Taurus is both
        clustered and spread out in age.}
    \label{fig:age_vel_comp}
\end{figure}

The population spans ages from 1 to 15~Myr, a wider range than the median ages of
$1$--$3$~Myr quoted in Section~\ref{sec:Taurus}. This is mainly a question of membership:
the clustering used here also recovers comoving groups of $\sim$12--15~Myr that
\citet{Luhman2023} assigns to neighboring associations rather than to Taurus itself.
The ages in the two catalogs are moreover determined with different methods. The bulk of the
sample is nonetheless young with most of the stars between 1 and 7~Myr, and the distribution
peaks at 3~Myr, in agreement with the median group ages of $1$--$3$~Myr reported by
\citet{Luhman2023}. The age distribution of the catalog is shown in Appendix~\ref{app:age_hist}.

Additionally, Figure~\ref{fig:CMD} shows a color-magnitude diagram (CMD) of the catalog,
colored by age to highlight the different subgroups, with isochrones for
1, 3, and 15~Myr overlaid \citep{Baraffe2015}. One star without available photometric data is
excluded from the diagram.

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.65\linewidth]{figures/CMD.pdf}
    \caption{CMD of the Taurus complex colored by the ages of the stars. Stars shown in grey
        have no age data available. Isochrones of ages 1, 3 and 15 Myr are overlaid.}
    \label{fig:CMD}
\end{figure}

The distribution of the measured radial velocities and their uncertainties gives a first
indication of the data quality.
Figure~\ref{fig:dataset_rv} shows a histogram of all available radial velocities alongside the
distribution of their reported uncertainties. The bulk of the measurements clusters around the
systemic velocity of Taurus, while a small number of clear outliers lie well outside the
plausible range, and a fraction of the measurements carry large uncertainties. Both features
motivate the filtering described below.

\begin{figure}[h!]
    \centering
    \begin{subfigure}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/rv_hist.pdf}
        \caption{Radial velocities.}
        \label{fig:dataset_rv_values}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/rv_err_hist.pdf}
        \caption{Radial-velocity uncertainties.}
        \label{fig:dataset_rv_errors}
    \end{subfigure}
    \caption{Distribution of the measured radial velocities (left) and their reported
        uncertainties (right) for the 180 stars with available radial velocities. Quality
        cuts are indicated.}
    \label{fig:dataset_rv}
\end{figure}

Before constructing the field, the sample is filtered to remove unreliable measurements.
The radial velocity data are distributed as a Gaussian curve with approximate mean and standard deviation of $16$ and $6~\mathrm{km\,s^{-1}}$ respectively.
A $5\sigma$ cut was chosen to exclude outliers from this distribution, which leaves radial velocity values in the range $[-14, 46]~\mathrm{km\,s^{-1}}$.
Furthermore, stars with uncertainties on their RV measurement greater than $15~\mathrm{km\,s^{-1}}$ are excluded from the likelihood as this marks the border between the bulk of measurements and the sparse tail of poorly constrained values.
Stars with $\mathrm{RUWE} > 2.5$ are additionally excluded, as elevated RUWE values are indicative of unresolved binary systems or a poor astrometric solution that may bias the velocity measurement \citep{Lindegren2021,Pearce2022RUWE}.
This value was chosen according to \citet{Fitton2022}, who argue for a more conservative binary threshold to account for the presence of disk material near young stars that could influence the single-star solution.
These cuts are applied only to the radial-velocity measurements, since proper motions carry substantially smaller uncertainties and are less strongly biased by unresolved binaries, which can significantly affect individual radial-velocity values \citep{CastroGinard2024}.
Due to the limited number of stars with radial-velocity measurements, these cuts are set deliberately lenient in order to retain as many data points as possible, excluding only the most severe outliers.


The data entering the
inference contain all 557 proper motion measurements and 123 radial velocity values
left after the quality cuts.
Figure~\ref{fig:dataset_positions} shows a three-dimensional representation of the stellar data, with
the stars whose radial velocities entered the inference marked separately.
The purpose of this visualization is to illustrate how few radial velocities remain usable: after the
quality cuts their share of the catalog drops from one third to less than a quarter.
Vector-based illustrations of the three-dimensional velocities are provided in Appendix~\ref{app:vectorplots}.

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.8\linewidth]{figures/rv_used.png}
    \caption{Three-dimensional heliocentric Galactic Cartesian positions of the 557
        catalog stars. Stars for which a radial-velocity measurement was used in the analysis
        (123 stars) are highlighted in green.}
    \label{fig:dataset_positions}
\end{figure}